apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: discovery
  namespace: __NAMESPACE__
spec:
  rules:
    - host: __HOST__
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: discovery
                port:
                  number: 80
